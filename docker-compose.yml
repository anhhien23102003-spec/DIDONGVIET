version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: genz-phone-landingpage
    ports:
      - "5000:5000"
    environment:
      PORT: 5000
      NODE_ENV: production
    restart: unless-stopped
